%
% template4.m
%
% Template user function with optional arguments
%
% Brian Moore
% brimoor@umich.edu
%
% November 10, 2017
%

function [x, y, z] = template4(x, y, z)
if ~exist('y', 'var'), y = 0; end
if ~exist('z', 'var'), z = 0; end
